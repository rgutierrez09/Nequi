
server:
  port: 8080
spring:
  application:
    name: "franchise"
  devtools:
    add-properties: true
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health"
  endpoint:
    health:
      probes:
        enabled: true

entries:
  reactive-web:
    basePath: "/franchise"